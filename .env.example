# Paddle Billing — client side (safe to expose, read-only token)
PUBLIC_PADDLE_CLIENT_TOKEN=test_xxxxxxxxxxxx
PUBLIC_PADDLE_ENV=sandbox   # or: production

# Paddle Billing — server side (webhook, KEEP SECRET)
# Used by Axum webhook service (not this Astro project)
# PADDLE_WEBHOOK_SECRET=pdl_ntfset_xxxxxxxx
